services:
  - type: web
    name: japan-guide-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: MAPBOX_TOKEN
        sync: false
